export const DEBUG = process.env.NODE_ENV !== 'production';

export const RML_OPTIONS = {
  toRDF: true,
  verbose: false,
  xmlPerformanceMode: false,
  replace: false,
};
export const RML_FILE = './src/rml/rml_sol.ttl';
